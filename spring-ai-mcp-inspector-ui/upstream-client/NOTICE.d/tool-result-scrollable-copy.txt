RETIRED in v2.3.0 (was: src/components/ToolResults.tsx — full tool result without
truncation, Copy button, scrollbar contained in the card). The v2 rewrite replaced
ToolResults.tsx with src/components/groups/ToolResultPanel plus
src/components/elements/ContentViewer, which render text blocks in full (no JsonView
100-char truncation) and expose a CopyButton overlay on every text block
(copyable={block.type === "text"}), inside a scroll body that keeps its scrollbars
within the card. The functionality of the v0.22.0 patch is provided by upstream v2.3.0
itself, so the local patch is dropped rather than ported. The v0.22.0 file
ToolResults.tsx and its covering tests no longer exist in the vendored tree.