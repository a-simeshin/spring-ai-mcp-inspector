This directory contains a vendored copy of modelcontextprotocol/inspector
client (Apache License 2.0).
Source: https://github.com/modelcontextprotocol/inspector
Vendored version: 0.22.0
Date: 2026-05-28

Local patches against upstream v0.22.0
======================================

1. vite.config.ts — `base: '/mcp-inspector/'` so the SPA is mountable
   under our Spring Boot starter's path.

2. index.html — bootstrap <script> seeds three upstream storage surfaces
   from the `window.__MCP_INSPECTOR_BOOTSTRAP` global that the backend's
   `InspectorIndexController` / `InspectorHandler` injects above it via
   `BootstrapHtmlRenderer`:
     - `inspectorConfig_v1` (localStorage) — persistent InspectorConfig
       entries keyed by `MCP_*` (e.g. `MCP_PROXY_FULL_ADDRESS`), in the
       `{value, label?, description?, is_session_item?}` shape expected by
       `initializeInspectorConfig` (`src/utils/configUtils.ts`).
     - `inspectorConfig_v1_ephemeral` (sessionStorage) — session-scoped
       entries in the same shape (e.g. `MCP_PROXY_AUTH_TOKEN`).
     - `lastTransportType` / `lastSseUrl` (localStorage, top-level) — the
       standalone keys consumed by `getInitialTransportType` and
       `getInitialSseUrl`.
   The seed merges into existing values so user-edited Settings survive a
   reload, and it resolves any path-only `proxyAddress` (e.g.
   `/mcp-inspector-api`) against the current origin before persisting it
   — the upstream client passes the address straight to `new URL(...)`
   in `useConnection.ts#checkProxyHealth`, which would otherwise throw
   on a relative URL. Covered end-to-end by `BootstrapRegressionE2ETest`
   in `spring-ai-mcp-inspector-demo` (the previous flat-shape seed crashed
   `initializeInspectorConfig` with `Cannot read properties of undefined
   (reading 'label')`).

3. src/components/Sidebar.tsx — import path fix
   (`../../../package.json` → `../../package.json`) so the build runs
   from this vendored layout. Also: render the app-title heading from a
   single template literal (`` {`MCP Inspector v${version}`} ``) instead of
   two adjacent JSX children (`MCP Inspector v{version}`). React produces
   one text node per child, so the upstream JSX rendered two sibling text
   nodes inside the `<h1>` and Selenide's `byText("MCP Inspector
   v0.22.0")` (xpath `text()[…] = "…"`) could not match — see
   `BootstrapRegressionE2ETest`.

4. src/App.tsx#readResource — fix stale right-pane after switching
   between resources and templates. Upstream early-returns on cache
   hit without syncing the singleton `resourceContent` state that
   ResourcesTab renders. Local fix: on cache hit, call
   `setResourceContent(resourceContentMap[uri])` before returning.
   Upstream: https://github.com/modelcontextprotocol/inspector/issues/1266

5. src/utils/configUtils.ts#getInitialSseUrl + index.html (bootstrap
   seed) — default the MCP target URL to the relative same-origin
   `/mcp` instead of the upstream absolute `http://localhost:3001/sse`,
   and strip an injected same-origin absolute back to a relative path.
   The proxy branch in `useConnection.ts` only puts this value into
   `searchParams.append("url", ...)` (never `new URL(sseUrl)`), so a
   relative `/mcp` is accepted and the default `?url=` carries no
   absolute `http://localhost`. This keeps the request clear of
   reverse-proxy WAF SSRF rules (OWASP CRS 934110/931100), which drop
   `?url=http://localhost...` before it reaches the app. The backend
   resolves the relative path to the loopback MCP endpoint server-side
   (`io.inspector.mcp.core.proxy.ProxyTargetResolver`), mirroring how
   springdoc-openapi serves the spec same-origin instead of pointing
   Swagger UI at an absolute `?url=`.

6. src/lib/auth.ts + src/lib/oauth-state-machine.ts +
   src/lib/hooks/useConnection.ts + src/components/OAuthFlowProgress.tsx
   — resolve a relative same-origin server URL against the current origin
   before handing it to `new URL(...)` as a base (`toAbsoluteServerUrl`).
   Patch 5 makes the default target the relative `/mcp`, and
   `new URL("/", "/mcp")` throws `Invalid URL`, which aborted OAuth
   discovery on the default configuration. The same applies wherever the
   value reaches the SDK: `auth()` and the state machine both funnel it
   into `resourceUrlFromServerUrl` -> `new URL(serverUrl)`.
   The proxy path keeps the relative value, so the WAF-safety of patch 5
   is unaffected.

7. src/App.tsx — explain the disabled Tasks tab. When the connected
   server does not advertise the `tasks` capability (Spring AI / MCP
   Java SDK 2.0.0 cannot declare it), the disabled `TabsTrigger` is
   wrapped in a keyboard-focusable span (`tabIndex={0}`, `title`,
   `aria-label`, `pointer-events-auto`) carrying a Radix Tooltip with
   the reason and a SEP-1686 documentation link. The wrapper is needed
   because the disabled trigger has `pointer-events:none`. Covered by
   src/__tests__/App.tasksTabTooltip.test.tsx (hover and focus paths;
   pointer-events polyfill lives in src/testUtils/pointerEventsPolyfill.ts).

8. src/App.tsx — wrap the tab bar below `sm` (Tailwind default 640px).
   The upstream TabsList base is `inline-flex h-9` with no wrapping,
   so at a 375px viewport the bar is ~1065px wide and the trailing
   tabs (Sampling/Elicitations/Roots/Auth/Metadata) render fully
   off-screen with no horizontal scroll (overflow visible) and no
   "more" affordance. Local fix (CSS-only, no JS/aria): `flex-wrap
   h-auto` on the TabsList lets the 11 triggers flow into multiple
   rows, and `sm:h-9 sm:flex-nowrap` restore the single-row upstream
   layout at >=640px (twMerge drops the base `h-9` in favour of
   `h-auto` below the breakpoint). Covered by the responsive tab-bar
   verification checklist at 375px/<640px (tester card t_ffb31393);
   the existing Selenide E2E suites keep exercising the >=640px layout
   and tab selection/routing.

9. src/components/ToolsTab.tsx — distinguish MCP spec-default hints
   from values declared by the server. Upstream renders a tool entry
   without an `annotations` object exactly like an annotated one, so
   the MCP spec defaults (readOnlyHint=false, destructiveHint=true)
   are displayed as if the server had asserted them. Local fix:
   `AnnotationBadges` gains a `declared` prop (true iff the selected
   entry carries an `annotations` object); when false, the
   Read-only/Destructive chips render muted with a "(default)" marker
   and their tooltip reads "Spec default, not declared by server".
   Entries with annotations render exactly as before. Covered by the
   ToolsTab vitest suite (spec-default vs declared scenarios) delivered
   with this change set; the demo server also emits `slowEcho` without
   annotations so the spec-default path stays demonstrable end-to-end.
