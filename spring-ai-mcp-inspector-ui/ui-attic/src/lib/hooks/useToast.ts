/**
 * Re-export of the toast hook provided by the Toast component module.
 * Lets feature code import from `lib/hooks` for consistency with other hooks.
 */
export { useToast } from '../../components/ui/Toast';
export type { ToastOptions, ToastVariant } from '../../components/ui/Toast';
